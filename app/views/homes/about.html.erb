<div style = "text-align: center">

		<h1>長野県にある小さな洋菓子店「ながのCAKE」</h1>

		<img src="/assets/nagano_cake.jpg" alt="非表示", style = "height: 500px;">

		<p style ="size: 10000px;">昨年始めたInstagramから人気となり、全国から注文が<br>
		来るようになったので通販サイトを作りました。</p>
		<span class="glyphicon glyphicon-map-marker" aria-hidden="true"></span>
		〒165-5744 長野県長野市x-x-xx

	</div>
	<script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['SECRET_KEY'] %> &callback=initMap" async></script>
	<script type="text/javascript">
	  var map;
	  var marker;
	  var geocoder;
	  function initMap() {
	    geocoder = new google.maps.Geocoder();
	    geocoder.geocode({
	    'address': '長野県長野市大字鶴賀七瀬南部３５８−３' //住所
	    }, function(results, status) {
	      if (status === google.maps.GeocoderStatus.OK) {
	      map = new google.maps.Map(document.getElementById('gmap'), {
	        center: results[0].geometry.location,
	        zoom: 17 //ズーム
	     });
	    marker = new google.maps.Marker({
	    position: results[0].geometry.location,
	    map: map
	    });
	    infoWindow = new google.maps.InfoWindow({
	    content: '<h4>ツールチップのタイトル</h4>'
	    });
	    marker.addListener('click', function() {
	      infoWindow.open(map, marker);
	    });
	    } else {
	      alert(status);
	    }
	  });
	}
	</script>
	<div id="gmap"></div>
