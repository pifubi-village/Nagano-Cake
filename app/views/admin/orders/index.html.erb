<!-- <h1>Admin::Orders#index</h1>
<p>Find me in app/views/admin/orders/index.html.erb</p> -->

<br>
<br>
<br>

<div class="container">
	<div class="row">

		<div class="col-md-6 col-md-offset-3">
            <div class="tyumon">
			  <h2><span class="gray">　注文履歴一覧　</span></h2>
			</div>

			<table class="table">
				<thead>
					<tr>
						<th>購入日時</th>
						<th>購入者</th>
						<th>注文個数</th>
						<th>注文ステータス</th>
					</tr>
				</thead>

				<tbody>

					<% @orders.each do |f| %>

					<tr>
						<td><%= link_to f.created_at.strftime("%Y-%m-%d %H:%M"), admin_order_path(f.id) %></td>
						<td><%= f.end_user.family_name %><%= f.end_user.first_name %></td>
						<% @aaa = 0 %>
						<% f.order_products.each do |d| %>
						<% @aaa += d.number.to_i %>
						<% end %>
						<td><%= @aaa %></td>
						<td><%= f.oeder_status %></td>
					</tr>

					<% end %>

				</tbody>
			</table>
			
			<div class='p-eu__paginate'>
			<%= paginate @orders %>
			</div>

		</div>

	</div>
</div>

